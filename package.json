{
  "name": "kitsune-a11y-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server: WCAG 2.2 audits (axe-core in a real browser) plus the Kitsune above-the-law checklist",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "axe-core": "^4.10.0",
    "playwright-core": "^1.48.0"
  },
  "devDependencies": {}
}
